```ruby
# Given root of binary tree, invert the tree then return its root
# For every node, left becomes right, right becomes left

# Iterate using DFS to hit bottom of tree
# On the way back, swap left/right
```
