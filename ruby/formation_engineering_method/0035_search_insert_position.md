```ruby
=begin
sorted array of DISTINCT integers, target value
return index if target is found, else return index where it would be
must be logn (binary search)
binary search, if return left when L == R if it isn't found
=end
```
