```ruby
=begin
Given nums (may contain duplicates)
return all possible UNIQUE permutations in any order
set to stop repeats
Generate all

Let's try NC's hashmap solution too, I think it's more efficient due to less repeated work + not needing sets
=end
```
