```ruby
=begin
nums, integer k
select index i and replace nums[i] with nums[i] - 1 (subtracting)
minimum # of operations to make sum of array divisible by k
Get the total sum of array
See how far it is from being divisible by k
Subtract until divisible by k
=end
```
