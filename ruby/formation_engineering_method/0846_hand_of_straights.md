```ruby
=begin
cards to rearrange into groups
so groupSize group consist of groupSize consecutive cards
hand[i] is value, return true if can be rearranged, otherwise false

Sort the array
Count the number of each value
Go through the array, make a group of size 3
Go to next value and try again
Return false if while completing a group, it doesn't hit the right size
=end
```
