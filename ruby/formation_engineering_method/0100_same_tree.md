```ruby
# Check if p and q are the same trees?
# If p.value == q.value, keep goin'
# Move left on p, move left on q, check if both are matching...
# Need to check if every subtree is the same
# Base case:
# - return false if one is nil but the other exists
# - return true if either exists
```
