```ruby
=begin
Trie aka prefix tree
Return true if word exists, false otherwise
starts with: true if if there's a word with said prefix, otherwise no

Insert:
I -> N -> S -> E -> R -> T
Each char -> hashmap
symbol WORD: true/false
=end
```
