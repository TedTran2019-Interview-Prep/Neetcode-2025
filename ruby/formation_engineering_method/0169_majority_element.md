```ruby
=begin
given array nums of size n, return majority element
element that appears more than n / 2 times
majority element always exists
count, return largest O(N) space, O(N) time
How to get O(1) space?
=end
```
